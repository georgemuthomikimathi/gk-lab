# GK Lab — E-Commerce

Digital drops, IMTAT courses, merch, and consulting.

```bash
npm install
cp .env.example .env.local
python3 ../scripts/scan-assets.py
npm run dev
```

Open http://localhost:3003
